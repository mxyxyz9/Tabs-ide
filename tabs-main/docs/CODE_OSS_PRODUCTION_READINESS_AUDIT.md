# Embedded Code-OSS production-readiness audit

Date: 2026-09-09

## Executive summary

The instability was not one bug. Tabs replaces VS Code's Electron main process with a custom host, and several contracts that stock VS Code assumes are always present were missing or only partially emulated. The observed failures map to five concrete causes:

1. `nativeHost` calls were silently accepted but returned `undefined`, including clipboard writes and external URL launch.
2. Extension OAuth URLs could leave the workbench but there was no route for the OS callback to reach the renderer's registered URI handler.
3. Assistant view containers changed location according to transient activation state, while webview resolution was also serialized behind the extension host's activation-complete acknowledgement. Together these produced a ready-but-empty iframe even after Claude had registered its provider.
4. The Copilot production bundle externalized runtime modules which packaging then omitted; webview resources were also proxied through `fetch(file://...)`, which intermittently returned empty or incorrectly typed worker/WASM bodies.
5. Tabs treated its outer AI-provider preference as Code-OSS extension enablement and wrote `chat.disableAIFeatures` differently into individual project profiles. Upstream reacts to that setting by stopping and restarting the extension host, canceling authentication and chat RPCs during startup.

These are host integration defects, not ordinary extension bugs. The repaired paths now follow the same boundaries as stock VS Code: native operations terminate in Electron main, URI callbacks are sent through the renderer `urlHandler` IPC channel, extension views have a stable container location, provider resolution can proceed as soon as registration occurs, and custom protocols return exact file bytes with explicit MIME types.

## Research baseline

- VS Code webviews run in isolated contexts and load local resources through controlled URI/protocol translation; worker assets must be delivered as real resource bytes with a valid content type. See the official [Webview extension guide](https://github.com/microsoft/vscode-docs/blob/main/api/extension-guides/webview.md).
- Authentication from an extension is expected to combine `window.registerUriHandler` with `env.asExternalUri`; in desktop builds, the application must own the protocol callback and return it to the correct window. See [Remote Development extension guidance](https://github.com/Microsoft/vscode-docs/blob/main/api/advanced-topics/remote-extensions.md?plain=1) and VS Code's [GitHub authentication implementation](https://github.com/microsoft/vscode/blob/main/extensions/github-authentication/src/githubServer.ts).
- Stock VS Code wires an Electron URL listener to a URL service and routes its `urlHandler` IPC channel to an active or explicitly identified window. See [VS Code Electron main](https://github.com/microsoft/vscode/blob/main/src/vs/code/electron-main/app.ts), [URL IPC routing](https://github.com/microsoft/vscode/blob/main/src/vs/platform/url/common/urlIpc.ts), and the [extension URL handler](https://github.com/microsoft/vscode/blob/main/src/vs/workbench/services/extensions/browser/extensionUrlHandler.ts).
- External browser launch is a native-host responsibility in the desktop workbench. See the stock [Electron workbench window implementation](https://github.com/microsoft/vscode/blob/main/src/vs/workbench/electron-browser/window.ts).

## Findings and disposition

| Severity | Area                                      | Evidence                                                                                                                                                                                                                                     | Root cause                                                                                                                                                                                                                                              | Disposition                                                                                                                                                                                                                                                                                                                          |
| -------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| P0       | Copy Path / clipboard                     | Context-menu action completed with no clipboard mutation                                                                                                                                                                                     | `writeClipboardText` fell through a passive `nativeHost` channel                                                                                                                                                                                        | Fixed with async Electron clipboard dispatch and tests                                                                                                                                                                                                                                                                               |
| P0       | Copilot activation                        | Runtime log: `Cannot find module 'dotenv'`; SDK directory import failure                                                                                                                                                                     | Production dependencies were declared as development-only; `.moduleignore` removed the SDK entrypoint                                                                                                                                                   | Fixed package metadata and packaging rule; compiled bundle imports successfully                                                                                                                                                                                                                                                      |
| P0       | OAuth/sign-in                             | Browser flow did not reliably open or return                                                                                                                                                                                                 | Missing `openExternal`; no `open-url` / second-instance to renderer `urlHandler` bridge                                                                                                                                                                 | Fixed outbound launch and inbound window-aware IPC routing; Clerk `tabs://app` URLs remain isolated                                                                                                                                                                                                                                  |
| P0       | Extension webviews                        | WASM `BufferSource` and MIME/protocol-read errors                                                                                                                                                                                            | Custom scheme delegated to `fetch(file://...)` in an isolated session                                                                                                                                                                                   | Fixed by direct byte reads and explicit MIME response; exact-byte WASM regression test added                                                                                                                                                                                                                                         |
| P0       | Intermittent extensions and Copilot auth  | Cold-reload stacks showed `ChatTeardownContribution` calling `updateRunningExtensions`, followed by `_doStopExtensionHosts`; GitHub default-account and chat-session calls then failed with `Canceled`                                       | Provider selection wrote `chat.disableAIFeatures=true` for Tabs into some global/project profiles and `false` for Copilot into others; this setting globally disables Code-OSS AI features rather than merely hiding Chat chrome                        | Fixed by keeping AI extension enablement on for both providers and using only the outer shell/auxiliary-bar visibility to switch UI; three instrumented reloads after the change completed GitHub provider initialization with no canceled RPC or extension-host teardown stack                                                      |
| P1       | Cross-project provider settings           | The live workbench is configured with `code-oss-desktop/shared-profile`, but provider changes wrote settings into inactive per-project profile remnants                                                                                      | The provider bridge and desktop launch configuration disagreed about the authoritative user profile                                                                                                                                                     | Fixed provider writes to target the shared desktop profile and main profile only; live Tabs → Copilot switching produced identical active settings and a subsequent cold reload completed without cancellation or host teardown                                                                                                      |
| P1       | Copilot attachment upload                 | `uploadFileViaMobileApi` fell through the native-host dispatcher                                                                                                                                                                             | GitHub's upload policy/S3/confirmation sequence must run in Electron main to avoid renderer CORS                                                                                                                                                        | Implemented the upstream-compatible three-step flow with strict policy validation, exact binary multipart data, bounded error bodies, confirmation-origin validation, and request-level tests                                                                                                                                        |
| P1       | File/Open workflows                       | `openWindow`, `openAgentsWindow`, and all four native picker-and-open commands fell through to `undefined`                                                                                                                                   | Stock Code-OSS delegates selection and window ownership to its Electron main process, but Tabs retained neither the owning project ID nor an adapter from new-window semantics to project tabs                                                          | Embedded windows now retain their project identity; selected files route through that project's authenticated extension channel, folders create/focus Tabs projects, and empty-window requests create Tabs tabs. Multi-root workspaces produce a visible limitation instead of a no-op                                               |
| P1       | Proxy diagnostics                         | Code-OSS network diagnostics received `undefined` from `resolveProxyWithPackage` and `readProxyConfigWithPackage`                                                                                                                            | The custom host implemented ordinary Electron proxy resolution but omitted the richer diagnostic contract used by Chat/Copilot troubleshooting                                                                                                          | Chromium proxy chains and standard proxy environment variables are now returned in Code-OSS's native diagnostic shape, with parsing tests                                                                                                                                                                                            |
| P1       | Claude/assistant placement and blank view | Live CDP trace showed `iframe.webview.ready` with no `content` message; Claude had registered `claudeVSCodeSidebarSecondary`, but the pane remained blocked awaiting activation completion                                                   | Container placement followed transient activation state and provider resolution was unnecessarily serialized behind the activation acknowledgement                                                                                                      | Fixed by preserving Claude's native fallback/secondary locations and starting activation and resolver lookup concurrently; live valid-workspace run reached the provider's authentication/render path                                                                                                                                |
| P1       | Native dialogs and OS services            | Standard workbench calls could resolve to `undefined`                                                                                                                                                                                        | Broad passive compatibility stub                                                                                                                                                                                                                        | Implemented message/open/save dialogs, process/window state, media permission, idle/battery, proxy and certificate paths; unsupported calls now log visibly                                                                                                                                                                          |
| P1       | Native lifecycle events                   | Focus, maximize/fullscreen, display, theme, suspend/resume, thermal and battery listeners never fired; an initial repair also duplicated owner-window focus across every embedded workbench                                                  | The entire native-host event channel returned `Event.None`, and an embedded view is a logical Code window even when several views share one Electron `BrowserWindow`                                                                                    | Replaced with Electron-backed events, using per-`WebContents` focus/blur and owner-window state events, including listener cleanup when an embedded workbench is destroyed                                                                                                                                                           |
| P1       | Integrated terminal stability             | Opening the terminal produced repeated unhandled `Canceled` rejections from `RequestStore.acceptReply`; after that was repaired, a clean run exposed `TypeError: names is not iterable` during pty-host startup                              | A successfully answered request canceled its timeout without consuming the cancellation rejection, and the embedded main-process configuration shim returned `undefined` instead of the registered default for `terminal.integrated.ignoreProcessNames` | Consumed the expected canceled-timeout rejection and restored the `[]` configuration default; a clean desktop restart plus live terminal creation reproduced neither failure                                                                                                                                                         |
| P1       | Editor save and restart persistence       | User could not tell whether editor mutations were actually durable                                                                                                                                                                           | This required end-to-end proof across the workbench, filesystem provider, disk, tab-state storage and reload—not inference from read/write unit tests                                                                                                   | Verified with a disposable workspace file: edited and saved through the embedded Monaco editor, observed clean tab and exact disk bytes, reloaded the full workbench, and observed the saved contents and active tab restored; probe removed afterward                                                                               |
| P1       | Deleted or moved project roots            | A persisted project referenced `/Users/rushil.dev/Desktop/Tabs-ide-cleanup-vscode-web-2026-04-28`, which no longer existed; Code-OSS still started and Git, Claude, CodeRabbit and filesystem-dependent extensions then failed independently | Workspace validation happened implicitly inside downstream services instead of at the session boundary                                                                                                                                                  | Session resolution now recovers a moved same-basename folder from bounded checkout-adjacent candidates, otherwise rejects with an actionable project-folder error before constructing a workbench or extension host; covered by recovery and rejection tests                                                                         |
| P1       | Claude remote sessions                    | Current log: `Failed to connect to remote server`                                                                                                                                                                                            | Extension/network-side request, after host/webview load succeeds                                                                                                                                                                                        | Not a host rendering failure; reproduce with network tracing and authenticated account before release                                                                                                                                                                                                                                |
| P2       | Hidden Code view font probe               | `SecurityError: Page needs to be visible` during startup                                                                                                                                                                                     | Workbench probes fonts while its embedded view is hidden                                                                                                                                                                                                | Non-fatal, but should be suppressed by delaying hidden-view initialization or upstream probe                                                                                                                                                                                                                                         |
| P2       | Native archive creation                   | `createZipFile` returned `undefined`, breaking support bundles and extension/agent workflows that request native archives                                                                                                                    | The custom main process did not load Code-OSS's native ZIP implementation                                                                                                                                                                               | Added bounded archive creation using Code-OSS's own ZIP writer, with exact source byte limits, duplicate/path-traversal rejection, missing-source policy, output cleanup, and tests. Existing-archive merging remains explicitly unsupported                                                                                         |
| P2       | Native-host completeness                  | The current `ICommonNativeHostService` contract exposes 103 callable methods, including platform-specific and standalone-window operations                                                                                                   | The embedded main process is a maintained compatibility surface rather than Code-OSS's stock Electron main                                                                                                                                              | All 103 methods now have an explicit policy: implemented behavior, an intentional embedded-product no-op, or a clear rejection. Tabs-native tab navigation replaces macOS window-tab routing; tracing/GPU diagnostics and Windows registry lookup are implemented. This is contract completeness, not a claim of full VS Code parity |

## Verification completed

- Desktop focused tests: 49 passing.
- Native-host callable-contract inventory: 103/103 methods have explicit policies; no method falls through the generic compatibility warning.
- Desktop TypeScript check: passing.
- Desktop production build: passing.
- Desktop Electron smoke test: passing.
- Code-OSS Tabs view tests: 3 passing.
- Code-OSS client typecheck: passing.
- Copilot packaging regression tests: 13 passing.
- Copilot production extension build: passing; packaged SDK, `dotenv`, and `source-map-support` are present and importable.
- Full Tabs monorepo typecheck: 12/12 packages passing.
- Full lint exits successfully; the repository still reports pre-existing warnings outside this change.
- Live reload logs after the packaging/protocol repairs no longer contain the prior Copilot module errors, browser-view channel failures, invalid window IDs, or webview WASM protocol errors.
- Live CDP validation proved Explorer **Copy Path of Active File** writes the expected absolute path to the macOS clipboard.
- Live CDP instrumentation proved the blank Claude iframe had completed its browser handshake while provider resolution was blocked; after the lifecycle repair, the actual nested Claude webview rendered its complete login-choice UI (Claude subscription, Anthropic Console, and cloud-provider options) against a valid project. A stale project record identified during testing now fails at session creation instead of launching a poisoned extension host; completion of the external authenticated flow still requires account-backed validation.
- An instrumented cold reload originally captured the provider-switch race end to end: Code-OSS stopped the extension host from `ChatTeardownContribution`, then GitHub/default-account and chat-session RPCs failed with `Canceled`. After making `chat.disableAIFeatures=false` invariant across providers and profiles, three reloads reached a complete workbench, registered GitHub and GitHub Enterprise authentication providers, and completed default-account initialization without any cancellation or extension-host-disposal stack.
- Live Copilot sign-in validation reached the native provider chooser and GitHub device-code prompt. **Copy & Continue to Browser** copied the exact one-time code to the macOS clipboard and launched the configured default browser (Arc); testing stopped before account authorization, so token persistence is not claimed.
- Live provider switching now updates the actual shared Code-OSS profile instead of inactive project-local remnants. Both application settings files retained `chat.disableAIFeatures=false`; switching back to Copilot enabled its native chrome, and the next instrumented reload registered GitHub authentication and completed default-account initialization without cancellations or unsupported native-host calls.
- Live Explorer validation also double-clicked `README.md` and observed it become the active pinned editor without preview state, covering the user's double-click concern against the rebuilt workbench.
- Expanded native parity after the runtime trace: fullscreen, window positioning/always-on-top/minimum size, represented filename/dirty-document state, text/find/buffer/image clipboard operations, native window handles and screenshots, process termination, port probing, native toast lifecycle, thermal and power-save APIs, relaunch/reload/close/quit, and DevTools commands now terminate in Electron instead of the compatibility fallback.
- A full desktop/backend restart loaded the rebuilt Code-OSS process, after which live creation of an integrated `zsh` terminal remained running without either the former `RequestStore` cancellation storm or the pty-host `ignoreProcessNames` rejection.
- A live save/reload persistence probe proved editor writes reach disk and restored editor/tab state survives a full workbench reload. The disposable probe was removed, and the pre-existing README remained byte-identical to Git after the test.
- Live Explorer testing on macOS created a new file and a new folder through Code-OSS's inline inputs, saved exact file contents, and moved both disposable entries to the system Trash through the native confirmation flow. Disk checks proved every transition and no probe remains in the workspace.
- Runtime unsupported-command tracing exposed `clearToasts` and `updateTouchBar`. Native toast creation/clear/cleanup is now implemented; `updateTouchBar` is an explicit embedded no-op because Code command IDs cannot be attached safely to Tabs' shared outer-window command router.

The repository's documented `vp` gate could not be run on this machine because the `vp` executable is not installed; the underlying typecheck and lint commands were run directly.

## Release blockers still requiring runtime proof

The implementation is materially more reliable, but production readiness should not be declared solely from unit tests. Before release, run a packaged-app matrix on macOS, Windows, and Linux that proves:

1. Explorer Copy Path and Copy Relative Path mutate the system clipboard.
2. Rename, Save As, and open dialogs work against local and remote workspaces. New File, New Folder, save, and delete/trash are now live-verified for a local macOS workspace but still require Windows/Linux and remote-workspace coverage.
3. GitHub/Copilot login completes from browser launch through callback and token persistence, including a second application launch on Windows/Linux.
4. Claude's supported secondary view and Copilot views remain in the auxiliary bar across cold start, reload, disable/enable, uninstall/reinstall, and delayed activation; Claude's primary fallback remains in the primary sidebar for older hosts.
5. A representative extension corpus exercises webviews, WASM workers, terminals, authentication providers, secrets, file watchers, and native dialogs.
6. Keep the automated native-host contract inventory at 100%, and runtime-test the platform-specific implementations on their owning OS.
7. Account and secret persistence survives restart without the historical safe-storage decryption errors.

The most important long-term change is procedural: treat the embedded Electron main-process surface as a versioned compatibility layer. Each Code-OSS update should diff the upstream native-host and URL/webview IPC contracts, with automated tests failing when a newly used method is not implemented.
