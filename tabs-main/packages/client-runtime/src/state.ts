/**
 * Shared atom primitives for the renderer. Domain-specific atoms live in the
 * web application because they also own UI-only persistence and navigation.
 */
export { Atom, AtomRegistry } from "effect/unstable/reactivity";
